<script setup>
import { Icon } from '@iconify/vue'
import { Home } from 'lucide-vue-next'
import { RefreshCw } from 'lucide-vue-next'
const colorMode = useColorMode()
</script>

<template>
  <div>
    <p class='font-semibold text-4xl flex flex-row items-center justify-center my-2'>S3 Browser</p>
    <slot />
  </div>

  <div class="absolute top-0 gap-5 left-0">
    <Button class="gap-2" @click="$router.push({path: '/'})">
      <Home />Home
    </Button>
    <Button class="gap-2" @click="refreshNuxtData()">
      <RefreshCw />
    </Button>
  </div>

  <div class="absolute top-0 right-0 mr-4 mt-4">
    <DropdownMenu>
      <DropdownMenuTrigger as-child>
        <Button variant="outline">
          <Icon icon="radix-icons:moon"
            class="h-[1.2rem] w-[1.2rem] rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
          <Icon icon="radix-icons:sun"
            class="absolute h-[1.2rem] w-[1.2rem] rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
          <span class="sr-only">Toggle theme</span>
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem @click="colorMode.preference = 'light'">
          Light
        </DropdownMenuItem>
        <DropdownMenuItem @click="colorMode.preference = 'dark'">
          Dark
        </DropdownMenuItem>
        <DropdownMenuItem @click="colorMode.preference = 'system'">
          System
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  </div>
</template>

<style scoped>
toastcolor {
  background: #FFFFFF;
  color: black
}
</style>
